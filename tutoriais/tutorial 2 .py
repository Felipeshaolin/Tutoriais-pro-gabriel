#agora vou te mostrar os comandos if:,else:, elif: et input().
gabriel = input( )
#input literalmente e um input que agente coloca.
#com esse codigo a coisa que vc escrever vai ser definida com q variavel de gabriel.
#a primera coisa que esse progama vai fazer é perguntar o numero, entao vc deve colocar um numero
if gabriel == 8:
  #se gabriel é igual a 8 ele faz algo
  #oque decide as acoes seguintes é a posicao do comando
  #se o codigo estiver aqui ele esta dentro do if:
#se o codigo estiver aqui ele esta fora do if:
  print("seu numero e 8!")
  #escreve seu numero é oito
else:
  #para mostrar que algo estra dentro de um else e do mesmo geito que o if
  # e para mostrar que um else 
  #else: e oque vai ser executado se o if: nao for verdade
  print("seu numero nao e 8")

#elif: é a junçao de else: e if:
#elif: é exatamente igual a isso:
#else:
   #if (condiçao):
#!o if: presiza de uma condicao para funcionar, e tipo na fala. se falamos "si" sem nada depois, é estranho!
  
